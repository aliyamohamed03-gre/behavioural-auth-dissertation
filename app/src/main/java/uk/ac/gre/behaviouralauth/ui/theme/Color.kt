package uk.ac.gre.behaviouralauth.ui.theme

import androidx.compose.ui.graphics.Color

//Defines the main app background colour.
val AppBackground = Color(0xFFF7F9FC)

//Defines the default card and section surface colour.
val AppSurface = Color(0xFFFFFFFF)

//Defines the main brand colour used for primary actions.
val AppPrimary = Color(0xFF1F5AA6)

//Defines the text/icon colour shown on primary-coloured elements.
val AppOnPrimary = Color(0xFFFFFFFF)

//Defines the background colour for the idle or unknown authentication state.
val StatusGreyBackground = Color(0xFFE0E0E0)

//Defines the text colour for the idle or unknown authentication state.
val StatusGreyText = Color(0xFF616161)

//Defines the background colour for trusted authentication status.
val StatusTrustedBackground = Color(0xFFC8E6C9)

//Defines the text colour for trusted authentication status.
val StatusTrustedText = Color(0xFF2E7D32)

//Defines the background colour for uncertain authentication status.
val StatusUncertainBackground = Color(0xFFFFE0B2)

//defines the text colour for uncertain authentication status.
val StatusUncertainText = Color(0xFF8A5A00)

//Defines the background colour for locked authentication status.
val StatusLockedBackground = Color(0xFFFFCDD2)

//Defines the text colour for locked authentication status.
val StatusLockedText = Color(0xFFC62828)

//Defines the colour used for high trust scores.
val ScoreGreen = Color(0xFF2E7D32)

//Defines the colour used for medium trust scores.
val ScoreAmber = Color(0xFFF57C00)

//Defines the colour used for low trust scores.
val ScoreRed = Color(0xFFC62828)

//Defines the background colour of the swipe capture area.
val SwipeAreaBackground = Color(0xFFE3F2FD)

//defines the dashed border colour of the swipe capture area.
val SwipeAreaBorder = Color(0xFF64B5F6)